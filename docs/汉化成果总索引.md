# 汉化与适配成果总索引（小白维护）

> 目的：所有已完成的汉化/适配/显示优化工作，一项不漏，标明"做了什么 + 改在哪 + 仓库在哪"。
> 更新规则：每完成一项新工作就追加一条；Hermes 升级后对照本清单逐项适配。

## 核心原则（老白 2026-08-07 确立）
- "不影响使用就全部汉化" — 用户可见内容全中文
- 本地验证没问题才推 GitHub 备份（backup，非 source of truth）
- 绝不"做了但找不到 / 有但不知道在哪"

---

## 一、飞书汉化（hermes-feishu-zh 仓库，main 分支）

**仓库**: `github.com/OLDBAI213/hermes-feishu-zh` (main = 最新完整版)
**工作区**: `Desktop/AI/feishu-zh-v20/`
**规则文件**: `patches/*.replacements.json`
**验证**: `verify.ps1` / `audit/feishu_localization_audit.py`

| 成果 | 规则文件 | 覆盖 | 状态 |
|---|---|---|---|
| 飞书消息中文化（102条） | `feishu-zh-v20.replacements.json` | adapter 59 + comment_rules 34 + meeting_invite 9 | ✅ 已应用 |
| 实时显示卡片 | `display-plus-v20.replacements.json` (22条) | 状态卡/进度卡/已收到思考 | ✅ 已应用 |
| 工具进度+显示配置 | `display-plus/stable.config.yaml` | tool_progress/footer | ✅ 已应用 |
| **setup 向导汉化（新补）** | `feishu-setup-comment-zh.replacements.json` (55条) | adapter setup 43 + comment prompt 12 | ✅ 已应用 |
| lark-cli 集成 | `plugins/lark-cli-toolbox/` | 12 工具 + 绑定 | ✅ 已配置 |

**live 位置**: `hermes-agent/plugins/platforms/feishu/` (adapter.py, feishu_comment.py, feishu_comment_rules.py, feishu_meeting_invite.py)
**实时显示**: `hermes-agent/gateway/feishu_realtime_display.py`
**配置**: `config.yaml` → `display.platforms.feishu.*`

### 飞书显示优化实测状态（2026-08-07 验证）
- 实时状态卡 ✅（render_status_card 被调用，输出"已收到思考/模型/服务商/上下文"）
- 工具进度卡 ✅（run.py __feishu_tool_started__/completed，显示"⏳工具名:摘要 → ✅(耗时)"）
- Markdown 表格 ✅（md tag 原生 GFM）
- DM 引用回复 ✅（reply_to 已实现）
- 话题群组 ✅（thread 锚点 + 99992402 回退）
- 已知死代码（无害保留）：render_process_card 无调用、_MARKDOWN_TABLE_RE 只用不定义

### 飞书三类汉化状态
| 类别 | 状态 |
|---|---|
| 重启/断开/上下文压缩 | ✅ 全中文（正在压缩/恢复会话/超时） |
| 工具调用 | ✅ 全中文（🧰执行过程N步/已收到思考） |
| 对话显示 | ✅ 全中文（含 setup 向导 + 评论 prompt 本轮补齐） |

---

## 二、TUI 汉化（hermes-tui-zh 仓库，master 分支）

**仓库**: `github.com/OLDBAI213/hermes-tui-zh` (master = 最新)
**工作区**: `Desktop/AI/github/hermes-tui-zh/`
**审计**: `scripts/audit-visible-text.mjs` + `allowlist/tui-visible-english.json`
**live**: `hermes-agent/ui-tui/src/`

| 成果 | 位置 | 状态 |
|---|---|---|
| slash 命令汉化 | `src/app/slash/commands/{core,ops,session}.ts` | ✅ 496→0 全清 |
| 会话状态+工具调用提示 | `src/app/createGatewayEventHandler.ts` + `createSlashHandler.ts` | ✅ |
| 计费/应用状态/组件层 | topup/subscription/billingOverlay/useMainApp 等 | ✅ 全清 |
| 启动横幅 | `hermes_cli/banner.py` + `skin_engine.py` | ✅ |
| 技能列表 description | 134 个技能 SKILL.md | ✅ 全中文 |
| 系统消息 | `agent/background_review.py` | ✅ |

**TUI 可见英文审计: 496 → 0 全部清零**

---

## 三、技能 SKILL.md description 汉化

- **范围**: 134 个顶层技能 description 全中文化（在 live skills/ 目录）
- **注意**: 保留 metadata.hermes.tags 英文（触发机制）；系统提示词 60 字符截断行为与之前一致
- **验证**: parse_frontmatter 全部通过，catalog 扫描 134/134 中文

---

## 四、系统消息汉化

- **agent/background_review.py**: "💾 Self-improvement review" → "💾 自我改进审查"；action 内容（已修补/已创建/已重写/已更新）
- **注意**: 不再兼容 Discord（老白明确不用），只保 TUI + 飞书；飞书 adapter 不识别该前缀，无影响

---

## 五、自进化 governor 修复（2026-08-07）

- **collect_signals.py**（skill 目录 + scripts 目录，已同步）修复 3 个 bug：
  1. tool-reliability 误报 → 只统计最近 24h + 尾部 400 行
  2. probe_cron_health `'list' object` 崩溃 → 适配 jobs.json 新结构 `{"jobs":[...]}`
  3. probe_cron_dependencies 同上
- **效果**: 信号采集干净（10 条 0 probe errors），error 计数从 434/2351 → 12/65

---

## 六、维护规则（Hermes 更新后）

**每次 Hermes 升级后，对照本清单逐项核查**：
1. 飞书 adapter/comment 是否被升级覆盖回英文 → 重跑规则文件恢复
2. TUI ui-tui 汉化是否被覆盖 → 从 hermes-tui-zh 重新应用 + rebuild
3. 技能 description 是否被覆盖 → 检查 skills/ 目录
4. 运行 `hermes doctor` 看新增警告
5. 更新本清单状态列

**版本适配记录**:
- v0.20.0 (2026-08-05): 飞书汉化迁移完成（102条）
- v0.20.0 (2026-08-07): TUI 全清 + setup/comment 补漏 + governor 修复
